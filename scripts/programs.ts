const programTable: Map<string, (args: string[], opts: string[]) => void> = new Map([
    ["name", name_]
]);

function name_(args: string[], opts: string[]) {
    // TODO: Generate some output to console
}
